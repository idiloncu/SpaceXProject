<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav"
    app:startDestination="@id/firstFragment">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.spacexproject.view.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment
        android:id="@+id/firstFragment"
        android:name="com.example.spacexproject.fragmentContainerView2"
        android:label="fragment_first"
        tools:layout="@layout/rocket_item" >
        <action
            android:id="@+id/action_firstFragment_to_descriptionFragment2"
            app:destination="@id/descriptionFragment" />
    </fragment>
    <fragment
        android:id="@+id/descriptionFragment"
        android:name="com.example.spacexproject.RocketDetailActivity.kt"
        android:label="fragment_description"
        tools:layout="@layout/rocket_description" >
        <action
            android:id="@+id/action_descriptionFragment_to_firstFragment2"
            app:destination="@id/firstFragment" />
    </fragment>
</navigation>